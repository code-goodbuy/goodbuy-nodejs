sudo: required

language: node_js
node_js:
  - 14

services:
  - docker
  # - mongodb 

before-install: 
  - docker build -t goodbuy/nodejs-webserver:travis .

script:  
  - npm run goodbuy/nodejs-webserver:travis mocha tests.js --timeout 5000

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

  - docker push goodbuy/nodejs-webserver:travis